#include <bits/stdc++.h>
#include "colormod.h" // namespace Color
using namespace std;
int main() {
for (int i = 0; i < 100; ++i)
{
	double r = ((double) rand() / (RAND_MAX)) ;
   // cout<<rand()<<endl;
    cout<<r<<endl;
    
}
}